import Funcs;

type fs` = Funcs;

arrow = 4;
add a b = a+b;
sub a b = a-b;
mult a b = a*b;
div a b = a/b;

inc = add 1;
dec = fs`->flip sub 1;

export {
    arrow,
    inc,
    dec,
    mult,
    div
}